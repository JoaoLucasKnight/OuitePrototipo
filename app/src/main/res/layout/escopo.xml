<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/titleCaminho"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceTitleLarge"
        android:fontFamily="@font/poppins_light"
        app:layout_constraintStart_toStartOf="@+id/recyclerView_escopo"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="This the way " />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView_escopo"
        android:layout_width="match_parent"
        android:layout_height="0dp"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/titleCaminho"
        tools:listitem="@layout/card_defaut" />

</androidx.constraintlayout.widget.ConstraintLayout>